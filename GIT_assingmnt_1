
git version --


git status

git add code.txt
git add output.txt
git add log.txt


git commit -m "done all"

git branch -M master


git remote add origin https://github.com/DadysCookin/GIT_Assingment.git

git remote -v

git push origin main







OPTIONAL: IF SSH KEY IS NOT AVAILABLE 
GIT IS TRYING TO CONNECT TO A REMOTE REPOSITORY SSH BUT DOESN'T HAVE THE PROPER ACCESS RIGHTS OR THE CORRECT SSH KEY IS NOT SET UP



ls -al ~/.ssh

ssh-keygen -t rsa -b 4096 -C "your_email@example.com"

eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_rsa

cat ~/.ssh/id_rsa.pub

ssh -T git@github.com






